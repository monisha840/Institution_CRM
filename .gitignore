node_modules/
.next/
out/
dist/
build/
.env*.local
.DS_Store
*.log
data/db.json
.claude/
# Legacy standalone cinematic sub-app (post-launch, never deployed from here).
Stansford CRM/
# Working note, not part of the deploy.
DEPLOYMENT.md
data/db.json.backup-*
.vercel
.env*

# Local Supabase dumps taken before migrations / re-seeds
backups/

# Working credentials written by the seeder
docs/credentials.md

# Alternate build dirs used when a second Next process runs alongside
.next-*
